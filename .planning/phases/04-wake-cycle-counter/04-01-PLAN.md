---
phase: 04-wake-cycle-counter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main.py
  - tests/test_rtc_memory.py
autonomous: true
requirements:
  - BATT-04
must_haves:
  truths:
    - "Wake counter increments on every button press wake"
    - "Wake counter value is printed to serial output"
    - "Counter wraps at 255 without corrupting other RTC memory fields"
  artifacts:
    - path: "main.py"
      provides: "increment_wake_counter() call in handle_button_wake() + serial print"
      contains: "increment_wake_counter"
    - path: "tests/test_rtc_memory.py"
      provides: "Integration tests for wake counter in handle_button_wake flow"
      contains: "test_wake_counter"
  key_links:
    - from: "main.py handle_button_wake()"
      to: "main.py increment_wake_counter()"
      via: "direct function call early in wake flow"
      pattern: "increment_wake_counter\\(\\)"
    - from: "main.py handle_button_wake()"
      to: "serial output"
      via: "print statement with wake counter"
      pattern: "Wake #"
---

<objective>
Integrate the existing wake cycle counter into the button press wake flow. The `increment_wake_counter()` and `load_wake_counter()` functions already exist from Phase 1. This plan wires them into `handle_button_wake()` and adds serial output.

Purpose: Enable diagnostics by tracking device usage since last power cycle.
Output: Wake counter incremented and printed on every wake, with tests.
</objective>

<execution_context>
@/Users/filippomerante/project/github/m5stack-atom-switchbot-lock-pro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/filippomerante/project/github/m5stack-atom-switchbot-lock-pro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@main.py
@tests/test_rtc_memory.py
@tests/conftest.py

<interfaces>
<!-- Functions already exist in main.py from Phase 1 -->

From main.py (lines 206-228):
```python
def increment_wake_counter():
    """Increment wake counter in RTC memory byte 10. Wraps at 255."""

def load_wake_counter():
    """Read wake counter from RTC memory. Returns 0 if unavailable."""
```

From main.py handle_button_wake() (line 687):
```python
def handle_button_wake(led):
    # Line 712: prints wake header
    # Line 728: load_wifi_config() — first RTC read
    # Line 789: battery_mv = read_battery_voltage()
    # Line 790-793: prints battery info
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add wake counter tests for handle_button_wake integration</name>
  <files>tests/test_rtc_memory.py</files>
  <read_first>
    - tests/test_rtc_memory.py
    - tests/conftest.py
    - main.py (lines 687-821, handle_button_wake function)
  </read_first>
  <behavior>
    - Test: increment_wake_counter() called during handle_button_wake produces counter=1 on first wake
    - Test: Serial output contains "Wake #" followed by the counter value
    - Test: Counter value printed alongside battery voltage in serial output
    - Test: Multiple increments produce sequential values (0->1->2)
  </behavior>
  <action>
Add integration tests to tests/test_rtc_memory.py in a new section "# Wake counter integration tests (BATT-04)":

1. `test_wake_counter_increment_on_button_wake()`:
   - Set up a valid v2 RTC layout (save_wifi_config first)
   - Call main.increment_wake_counter()
   - Assert main.load_wake_counter() == 1
   - Call main.increment_wake_counter() again
   - Assert main.load_wake_counter() == 2

2. `test_wake_counter_serial_output(capsys)`:
   - Set up valid v2 RTC layout
   - Call main.increment_wake_counter()
   - Print using the same format handle_button_wake will use: `print(f"Wake #{main.load_wake_counter()}")`
   - Assert "Wake #1" in captured stdout

3. `test_wake_counter_preserves_battery_voltage()`:
   - Set up valid v2 RTC layout with save_wifi_config
   - Save battery voltage 3850 via save_battery_voltage(3850)
   - Call increment_wake_counter()
   - Assert load_battery_voltage() still returns 3850
   - Assert load_wake_counter() returns 1

Note: These tests verify the RTC functions work correctly for integration. The actual handle_button_wake integration is verified by Task 2's automated check.
  </action>
  <verify>
    <automated>cd /Users/filippomerante/project/github/m5stack-atom-switchbot-lock-pro && make test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "test_wake_counter" tests/test_rtc_memory.py returns 3 or more
    - grep "Wake #" tests/test_rtc_memory.py succeeds
    - grep "load_battery_voltage" tests/test_rtc_memory.py | grep -c "3850" returns at least 1
    - make test passes with 0 failures
  </acceptance_criteria>
  <done>Three new wake counter integration tests pass, verifying increment, serial format, and battery voltage preservation.</done>
</task>

<task type="auto">
  <name>Task 2: Wire wake counter into handle_button_wake</name>
  <files>main.py</files>
  <read_first>
    - main.py (lines 687-821, handle_button_wake function)
  </read_first>
  <action>
Modify handle_button_wake() in main.py to increment and print the wake counter:

1. **Increment early** — Add `increment_wake_counter()` call immediately after the wake header prints (after line 714 `print("=" * 50)`), before WiFi connect. This is safe because it is a simple RTC byte write with no system heap impact.

2. **Print counter** — After the increment, add:
   ```python
   wake_count = load_wake_counter()
   print(f"Wake #{wake_count}")
   ```

3. **Combined battery + counter output** — In the battery print section (around lines 790-794), change the battery print to include the wake count for a single diagnostic line. Replace:
   ```python
   if battery_mv > 0:
       print(f"Battery: {battery_mv}mV")
   ```
   with:
   ```python
   if battery_mv > 0:
       print(f"Battery: {battery_mv}mV | Wake #{wake_count}")
   ```
   To make `wake_count` available at that point, either use the variable from step 2 (it's in the same function scope) or call `load_wake_counter()` again — prefer reusing the variable.

   Move the `wake_count` variable assignment to be available in the broader scope of handle_button_wake (it already is if placed at the top as described in step 2).

4. **Do NOT** add increment on cold boot (reset_cause != DEEPSLEEP_RESET) — per CONTEXT.md this is Claude's discretion; cold boot should not count since it resets RTC memory anyway.

Do NOT add any imports (increment_wake_counter and load_wake_counter are already defined in main.py).
Do NOT modify any code before urequests.post() that could affect the system heap. The RTC byte write via machine.RTC is safe.
  </action>
  <verify>
    <automated>cd /Users/filippomerante/project/github/m5stack-atom-switchbot-lock-pro && make test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "increment_wake_counter()" main.py | grep -v "def " | grep -v "#" returns at least 1 match (call site in handle_button_wake)
    - grep "Wake #" main.py returns at least 1 match
    - grep -A2 "Battery:.*mV" main.py | grep "Wake #" returns at least 1 match (combined output line)
    - make test passes with 0 failures
  </acceptance_criteria>
  <done>Wake counter increments on every button press wake, counter printed to serial early in flow and alongside battery voltage. All existing tests still pass.</done>
</task>

</tasks>

<verification>
1. `make test` — all tests pass (existing + 3 new wake counter tests)
2. `grep "increment_wake_counter()" main.py` — shows call in handle_button_wake
3. `grep "Wake #" main.py` — shows serial output with counter
4. `grep "test_wake_counter" tests/test_rtc_memory.py` — shows 3+ new test functions
</verification>

<success_criteria>
- Wake counter in RTC memory byte 10 increments on every button press wake
- Counter value printed to serial alongside battery voltage
- Counter wraps at 255 without corrupting battery voltage or WiFi BSSID
- All 55+ tests pass (53 existing + 3 new)
</success_criteria>

<output>
After completion, create `.planning/phases/04-wake-cycle-counter/04-01-SUMMARY.md`
</output>
