---
phase: 08-logging-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main.py
  - config_template.py
  - tests/test_logging.py
  - tests/conftest.py
autonomous: true
requirements:
  - PWR-02
  - PWR-03

must_haves:
  truths:
    - "With LOG_LEVEL='verbose' (or absent), all serial output appears exactly as before"
    - "With LOG_LEVEL='minimal', only errors, battery voltage, and wake counter are printed"
    - "With LOG_LEVEL='silent', zero print output occurs during a wake cycle"
    - "Default behavior with no LOG_LEVEL in config.py is identical to current verbose output"
  artifacts:
    - path: "main.py"
      provides: "log() function and all print() calls replaced with log()"
      contains: "def log("
    - path: "main.py"
      provides: "LOG_LEVEL loaded from config with try/except fallback"
      contains: "LOG_LEVEL"
    - path: "config_template.py"
      provides: "LOG_LEVEL documented as optional constant"
      contains: "LOG_LEVEL"
    - path: "tests/test_logging.py"
      provides: "Tests for log level filtering"
      contains: "def test_"
  key_links:
    - from: "main.py"
      to: "config.py"
      via: "try/except import of LOG_LEVEL"
      pattern: "from config import.*LOG_LEVEL"
    - from: "main.py log()"
      to: "main.py _LOG_LEVEL global"
      via: "level comparison in log function"
      pattern: "def log\\("
---

<objective>
Add configurable serial output verbosity to main.py via LOG_LEVEL in config.py.

Purpose: Reduce UART transmission time in production deployments, saving power on every wake cycle. Each print() call takes time on the serial bus at 115200 baud; silencing output saves ~50-100ms per cycle.
Output: log() function in main.py, all print() calls replaced, LOG_LEVEL in config_template.py, test coverage.
</objective>

<execution_context>
@/Users/filippomerante/project/github/m5stack-atom-switchbot-lock-pro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/filippomerante/project/github/m5stack-atom-switchbot-lock-pro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@main.py
@config_template.py
@tests/conftest.py

<interfaces>
<!-- Config pattern established in main.py for optional constants -->
From main.py (existing pattern for optional config imports):
```python
# Pattern: try/except with fallback default (same as WIFI_STATIC_IP)
try:
    from config import WIFI_STATIC_IP
    wlan.ifconfig(WIFI_STATIC_IP)
except (ImportError, AttributeError):
    pass
```

From config_template.py (existing optional constant pattern):
```python
# Static IP (optional, saves ~500ms-1s per connection by skipping DHCP)
# WIFI_STATIC_IP = ("192.168.1.100", "255.255.255.0", "192.168.1.1", "8.8.8.8")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create log() function with level filtering and tests</name>
  <files>main.py, tests/test_logging.py, tests/conftest.py</files>
  <read_first>
    - main.py (lines 1-80 for imports/config section, lines 646-660 for constants section)
    - tests/conftest.py (full file for stub patterns)
    - config_template.py
  </read_first>
  <behavior>
    - Test 1: log("hello", level="verbose") prints "hello" when _LOG_LEVEL is "verbose"
    - Test 2: log("hello", level="verbose") prints nothing when _LOG_LEVEL is "minimal"
    - Test 3: log("hello", level="minimal") prints "hello" when _LOG_LEVEL is "minimal"
    - Test 4: log("hello", level="minimal") prints nothing when _LOG_LEVEL is "silent"
    - Test 5: log("hello", level="verbose") prints "hello" when _LOG_LEVEL is "silent" returns nothing
    - Test 6: log() with end="" kwarg passes through to print (for "." progress dots)
    - Test 7: log() with no level defaults to "verbose"
    - Test 8: Default _LOG_LEVEL is "verbose" when config has no LOG_LEVEL
    - Test 9: _LOG_LEVEL loaded from config when LOG_LEVEL is set in config module
    - Test 10: Invalid LOG_LEVEL value (e.g., "debug") falls back to "verbose"
  </behavior>
  <action>
    1. In tests/conftest.py: Do NOT add LOG_LEVEL to the fake config module (so default "verbose" is tested).

    2. Create tests/test_logging.py with tests for the log() function:
       - Import main module, monkeypatch main._LOG_LEVEL to test each level
       - Use capsys to capture print output
       - Level hierarchy: "verbose" prints everything, "minimal" prints level="minimal" only, "silent" prints nothing
       - Test that end="" and other kwargs pass through to print()

    3. In main.py, add the logging infrastructure AFTER the config import block (around line 80) but BEFORE any function that uses print():

       a. Load LOG_LEVEL from config with fallback:
          ```python
          try:
              from config import LOG_LEVEL as _LOG_LEVEL
              if _LOG_LEVEL not in ("verbose", "minimal", "silent"):
                  _LOG_LEVEL = "verbose"
          except (ImportError, AttributeError):
              _LOG_LEVEL = "verbose"
          ```

       b. Define the log() function:
          ```python
          # Log level numeric mapping for fast comparison
          _LOG_LEVELS = {"silent": 0, "minimal": 1, "verbose": 2}

          def log(*args, level="verbose", **kwargs):
              """Print with log level filtering. Levels: verbose, minimal, silent."""
              if _LOG_LEVELS.get(level, 2) <= _LOG_LEVELS.get(_LOG_LEVEL, 2):
                  print(*args, **kwargs)
          ```

       IMPORTANT: This is a simple function definition, NOT a module-level import. Safe for mbedTLS heap per CLAUDE.md.

    4. In config_template.py, add after the WIFI_STATIC_IP comment:
       ```python
       # Serial output verbosity (optional, default: "verbose")
       # "verbose" = all output (default, backward compatible)
       # "minimal" = errors + battery voltage + wake counter only
       # "silent"  = no serial output (saves UART time in production)
       # LOG_LEVEL = "minimal"
       ```

    5. Run tests to verify all pass.
  </action>
  <verify>
    <automated>cd /Users/filippomerante/project/github/m5stack-atom-switchbot-lock-pro && make test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "def log(" main.py
    - grep -q "_LOG_LEVEL" main.py
    - grep -q "LOG_LEVEL" config_template.py
    - grep -q "def test_" tests/test_logging.py
    - grep -q "from config import LOG_LEVEL" main.py
  </acceptance_criteria>
  <done>log() function exists in main.py with level filtering, LOG_LEVEL loaded from config with "verbose" default, config_template.py documents the option, all tests pass including new test_logging.py</done>
</task>

<task type="auto">
  <name>Task 2: Replace all print() calls with log() at appropriate levels</name>
  <files>main.py</files>
  <read_first>
    - main.py (full file — need to see every print() call)
  </read_first>
  <action>
    Replace every `print()` call in main.py with `log()`, assigning the correct level to each.

    **Level assignment rules:**

    LEVEL="minimal" (printed in both verbose and minimal modes) — errors, warnings, battery, wake count, critical status:
    - Line 77-78: config.py not found ERROR (these 2 stay as print() since log() may not exist yet at import time — actually keep as print() since they run before log() is defined)
    - Line 91-92: NTP sync failure with WARNING
    - Line 108-109: Clock still invalid after NTP
    - Line 114: Unable to verify system time
    - Line 147: Could not save Wi-Fi config error
    - Line 252: ADC read failed
    - Line 259: Low battery WARNING
    - Line 463: No response from API
    - Line 480-481: HTTP status + Response text
    - Line 487: Auth failed 401
    - Line 491: Exception sending command
    - Line 571: Wi-Fi connection timeout
    - Line 721: Wake #{wake_count}
    - Line 746: Cannot connect to Wi-Fi
    - Line 798: Battery voltage + wake count line
    - Line 801: Battery read failed
    - Line 807: Door locked successfully
    - Line 810: Door unlocked successfully
    - Line 813: Auth error 401
    - Line 816: Time sync error
    - Line 819: API error

    LEVEL="verbose" (printed only in verbose mode) — informational, progress, diagnostics:
    - Line 87: "Synchronizing time via NTP..."
    - Line 89: Time synchronized success
    - Line 103: Clock seems unsynchronized
    - Line 452: Retry message
    - Line 459: Sending LOCK/UNLOCK command
    - Line 523-524: Already connected to Wi-Fi + IP
    - Line 531: Static IP info
    - Line 535: Static IP rejected
    - Line 541: Fast reconnect progress (with end="")
    - Line 551: timeout
    - Line 558-559: OK + IP
    - Line 562: Fast reconnect failed
    - Line 565: Connecting to Wi-Fi
    - Line 579: "." progress dots (with end="")
    - Line 581-582: Connected to Wi-Fi + IP
    - Line 591: Cached channel
    - Line 606: Cached channel (scan)
    - Line 608: Could not cache Wi-Fi config
    - Line 620-623: Entering deep sleep info (4 lines)
    - Line 714-716: Wake header banner (3 lines)
    - Line 728-729: Button held + Action
    - Line 738: Fast reconnect available
    - Line 761: RTC time valid
    - Line 763: RTC time invalid
    - Line 772: NTP sync failed warning (this one keep as minimal since it affects reliability)
    - Lines 852-861: Fresh boot banner (all verbose)

    **KEEP as raw print() (not converted):**
    - Lines 77-78: config.py not found — these run during module import before log() is defined. They MUST remain as print().

    **Implementation approach:**
    - Global find-and-replace of `print(` with `log(` across the file
    - Then add `, level="minimal"` to the minimal-level calls
    - Then restore lines 77-78 back to `print()`
    - Verbose calls need no level= kwarg since "verbose" is the default
    - Preserve all `end=""` kwargs (they pass through log to print)

    **Verify no raw print() remains** except the 2 config error lines.
  </action>
  <verify>
    <automated>cd /Users/filippomerante/project/github/m5stack-atom-switchbot-lock-pro && python3 -c "
import re
with open('main.py') as f:
    lines = f.readlines()
# Count remaining raw print() calls (should be exactly 2: config error lines 77-78 area)
raw_prints = [(i+1, l.strip()) for i, l in enumerate(lines) if re.search(r'\bprint\s*\(', l) and 'def log' not in l]
print(f'Raw print() calls: {len(raw_prints)}')
for ln, txt in raw_prints:
    print(f'  Line {ln}: {txt}')
assert len(raw_prints) == 2, f'Expected 2 raw prints (config error), got {len(raw_prints)}'
# Count log() calls
log_calls = [(i+1, l.strip()) for i, l in enumerate(lines) if re.search(r'\blog\s*\(', l) and 'def log' not in l and '_LOG_' not in l]
print(f'log() calls: {len(log_calls)}')
assert len(log_calls) >= 45, f'Expected 45+ log() calls, got {len(log_calls)}'
# Verify minimal level calls exist
minimal_calls = [l for _, l in log_calls if 'level=\"minimal\"' in l]
print(f'Minimal-level calls: {len(minimal_calls)}')
assert len(minimal_calls) >= 15, f'Expected 15+ minimal calls, got {len(minimal_calls)}'
print('ALL CHECKS PASSED')
" && make test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'log(' main.py returns 45+ (all former print calls converted)
    - grep -c 'level="minimal"' main.py returns 15+ (error/battery/status calls)
    - grep -c 'print(' main.py returns exactly 3 (2 config error lines + 1 inside log() definition)
    - make test passes with 0 failures
  </acceptance_criteria>
  <done>All print() calls in main.py replaced with log() at correct levels. "verbose" mode output identical to before. "minimal" shows only errors, battery, and critical status. "silent" suppresses all output. All existing tests pass (they still see output because _LOG_LEVEL defaults to "verbose").</done>
</task>

</tasks>

<verification>
1. `make test` — all existing tests pass (log() defaults to verbose, output unchanged)
2. New test_logging.py tests pass for all three log levels
3. No raw `print()` in main.py except the 2 config-not-found error lines
4. config_template.py documents LOG_LEVEL with all three options
</verification>

<success_criteria>
- LOG_LEVEL="verbose" (or absent): Output identical to current firmware
- LOG_LEVEL="minimal": Only errors, battery voltage, wake count, and lock/unlock result printed
- LOG_LEVEL="silent": Zero serial output during wake cycle
- All 53+ existing tests pass unchanged
- New test_logging.py covers level filtering logic
</success_criteria>

<output>
After completion, create `.planning/phases/08-logging-control/08-01-SUMMARY.md`
</output>
